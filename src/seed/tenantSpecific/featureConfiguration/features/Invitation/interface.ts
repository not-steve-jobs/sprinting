/**
 * Feature to control the Invitations
 *
 * @param {number} daysValid - control the expiration days for the user invitations
 */
export interface InvitationFeatureConfiguration {
  daysValid: number;
}
