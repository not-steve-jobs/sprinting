/**
 * List with demo data used to generate "random" seeds
 */

import {WorkplaceStatus} from 'src/modules/workplace/workplace.enum';

export const DEMO_WORK_PLACE_STATUSES: WorkplaceStatus[] = Object.values(WorkplaceStatus);
